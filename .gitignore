node_modules
.next
.env
.env.development
.env.test
.env.production
.env.local
.env.docker
.env.docker.dev
# Arquivos de operador apontando para o Neon (cloud). Nunca versionar.
.env.neon*
*.db
*.db-journal
prisma/dev.db
npm-debug.log*
.DS_Store
*.tsbuildinfo
backups/
backup_upa_*.sql
*.zip
# Gerenciador oficial: pnpm
/package-lock.json
pnpm-debug.log*
# --- Backup / restore (Issue #11) ---
# "backups/" ja esta ignorado acima: dumps, hashes, manifestos e logs de
# backup nunca sao versionados.
# Configuracao local do operador (nomes de container/banco e caminhos):
scripts/backup/backup.config.psd1
# Configuracao e credenciais do rclone (OAuth do Google Drive + chaves crypt):
rclone.conf
*.rclone.conf
